restrict -4 default kod nomodify notrap nopeer noquery
restrict -6 default kod nomodify notrap nopeer noquery
restrict 127.0.0.1
restrict ::1
<% scope.lookupvar('ntp::real_ntp_servers').each do |ntpServer| -%>
server <%= ntpServer %>
<% end -%>
